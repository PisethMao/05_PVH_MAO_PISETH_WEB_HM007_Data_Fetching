version: "3.9"
services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs-data-fetching
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always
